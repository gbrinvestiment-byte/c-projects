#include <stdio.h>
int main() {
    int nota = 75;
    if (nota > 60) {
        printf("Parabéns, você passou!.\n");
    } else {
        printf("Infelizmente você reprovou.\n");
    } if (nota == 60) {
        printf("Você passou por um triz!.\n");
    }

    return 0;
}